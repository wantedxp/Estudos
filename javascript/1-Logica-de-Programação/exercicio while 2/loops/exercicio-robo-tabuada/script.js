let numero = prompt("Digite um numero")

for (let multiplicador = 1; multiplicador <= 20; multiplicador++) {
    let resultado = numero * multiplicador
    alert(`O resultado de ${numero} * ${multiplicador} é ${resultado}`)
}
